import { MainLayout } from '@/layout/main-layout';
import { styled } from '@mui/material';

export const AboutPage = () => {
  return (
    <MainLayout>
      <Root className="container"></Root>
    </MainLayout>
  );
};

const Root = styled('div')``;
